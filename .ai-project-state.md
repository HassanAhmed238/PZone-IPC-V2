# PZone Control System - AI Project State

This file is automatically maintained by the AI (Gemini/Opus). **Do not delete.**
It serves as the highly compressed communication layer for context handover between different AI models.

## 1. Project Context & Stack
*   **Name:** PZone Control System
*   **Description:** Construction / Engineering ERP & Project Management
*   **Frontend:** React 18, Vite, TypeScript, Tailwind CSS, shadcn/ui, `react-router-dom`, `react-query`
*   **Backend:** Supabase (Postgres, Storage, Auth, Edge Functions)
*   **Database Tooling:** Heavy usage of raw SQL migrations and `.cjs` server scripts for maintenance.
*   **Design Constraints:** Premium UI, Lucide icons, Dark/Light mode support, *ARABIC (ar)* primary locale for formatting and UI.

## 2. Project Architecture & UI Screens
The application relies on RBAC (Role-Based Access Control) with roles like `admin`, `pm`, `cost_control`, `contracts`, `planning`, `commercial`.

**Key Screens (`src/pages/`):**
*   `DashboardPage`
*   `MasterDataPage` (Setup/Configuration)
*   `UserManagementPage` (RBAC)
*   `OngoingProjectsProgressPage`
*   `SiteProgressPage`
*   `ClientInvoicesPage`
*   `CostControlPage`
*   `contracts/` (Includes contract lists, ContractAnalyzerPage, NotebookLMAnalyzerPage)
*   `projects/`, `budget/`, `tenders/` (Modules)
*   Authentication (`LoginPage`, `SignupPage`, `ResetPasswordPage`)

## 3. Backend State & Data Contracts
**Active Supabase Features:**
*   **Storage:** `contracts` bucket (RLS protected).
*   **Edge Functions:** Required for hitting Gemini / Groq APIs securely.
*   **Key Tables:**
    *   `contracts` (id, title, contract_type, employer_name, contractor_name, contract_value, currency, file_url, ai_analysis_results, ai_extracted, status)
    *   `contract_clauses`
    *   `contract_amendments`
    *   `ongoing_projects`
    *   `invoices` (IPC invoices — core IPC data with deductions, VOs, tax, fluctuation, approval)
    *   `ipc_projects` (IPC project registry with variation orders)
    *   `collection_transactions` (monthly collection ledger — dedupe_key, status lifecycle)
    *   `cash_flow_transactions` (cash in/out actuals)
    *   `cash_flow_forecasts` (cash in/out forecasts with probability_pct)
    *   `board_share_tokens` (IPC board snapshot sharing)
    *   `profiles` (Users and roles)

**Important Migration History:**
*   Recently added Site Progress module, BOQ tracking formats, and Contract Analysis tracking.

## 4. Current Work / Shared State
*   **IPC Milestones:** M1 (Safety & Cleanup), M2 (TTL Recovery), M3 (Compiler Annotations), M4 (Filter Memoization) are **DONE**. M5 (Verification & E2E) is PLANNED.
*   **Collections Module:** Phase A complete (transaction ledger, dedupe, import validation). Phase B (reconciliation dashboards) in progress.
*   **Backend Focus:** Ready for new Supabase schema adjustments or Edge Function deployments.
*   **Data Contracts Pending:** None currently active.

---
*Last Updated: 2026-06-12*
*Model Authoring this update: Claude Opus (Antigravity)*
